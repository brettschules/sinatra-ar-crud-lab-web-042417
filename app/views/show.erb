<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>A Blog Post</title>
  </head>
  <body>
    <h1>My Post</h1>
      <h2><%= @post.name %></h2>
        <p><%= @post.content %></p>

        <form action='/posts/<%= @post.id %>'
          method="POST">
          <input name="_method" id="hidden"
          type="hidden" value="delete">
          <input type="submit" value="delete">



</html>
